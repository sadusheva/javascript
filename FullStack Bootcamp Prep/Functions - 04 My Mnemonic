/*Write a function myMnemonic that accepts up to four strings. myMnemonic should
return a new string of the first characters for each string argument.

Note that the user may not pass in all four strings! */

function myMnemonic(...){
  let mnemonic = '';
  
 for(let i=0; i<arguments.length; i++){
   let letter = arguments[i].charAt();
   mnemonic += letter;
 }
 return mnemonic;
} 
